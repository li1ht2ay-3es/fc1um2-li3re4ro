on:
  workflow_dispatch:
  schedule:
    - cron: '0 5 * * *'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: li1ht2ay-3es/fc1um2-li3re4ro@cron
        with:
          token: ${{ secrets.token }}
