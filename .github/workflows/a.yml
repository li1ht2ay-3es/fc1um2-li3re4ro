on:
  push:
  workflow_dispatch:


jobs:

  build_libretro:
    strategy:
      matrix:
        arch: [x86, x64]
      fail-fast: false


    runs-on: ubuntu-latest

    steps:
      - uses: li1ht2ay-3es/fc1um2-li3re4ro@build-libretro
        with:
          token: ${{ secrets.token }}
          arch: ${{ matrix.arch }}
